<UserControl x:Class="HCL.CS.SF.DemoClientWpfApp.View.EmailVerication"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:intr="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodel="clr-namespace:HCL.CS.SF.DemoClientWpfApp.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:EmailVericationViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="600" HorizontalAlignment="Center">

    <UserControl.DataContext>
        <viewmodel:EmailVericationViewModel/>
    </UserControl.DataContext>
    
    <intr:Interaction.Triggers>
        <intr:EventTrigger EventName="Loaded">
            <intr:InvokeCommandAction Command="{Binding OnLoadCommand}"/>
        </intr:EventTrigger>
    </intr:Interaction.Triggers>
    
        <Grid x:Name="LayoutRoot">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel  Background="Transparent">
            <Grid Height="auto" Width="auto"   Margin="10,10,10,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="10"/>
                         <ColumnDefinition Width="auto"/>

                </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                <Label  Grid.Column="1"  Grid.Row="0" FontSize="20" Foreground="#85409d" Margin="0 0 0 20"   HorizontalAlignment="Left" Name="VerifyEmaillable" >
                        <Bold>Verify Account</Bold>
                    </Label>
                <TextBlock Grid.Column="1" Foreground="#1493D6" FontWeight="Bold" Grid.Row="1"  Name="textVerifyEmai" Text="Please enter the email verification code :" VerticalAlignment="Top"/>
                    <TextBox Grid.Column="2" Width="200" Height="25"  Grid.Row="1"  Name="EmailVerificationcode" Margin="0 0 0 10" Text="{Binding EmailVerificationCode, Mode=TwoWay}" />
                    <Button Grid.Column="4" FontWeight="Bold" Grid.Row="1"  HorizontalAlignment="Left" Content="Resend Email" Cursor="Hand" Command="{Binding Path=ResendEmailCommand}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <TextBlock TextDecorations="Underline">  
                    <ContentPresenter />
                                </TextBlock>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Foreground" Value="Blue" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter Property="Foreground" Value="Red" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                <TextBlock Grid.Column="1"  Grid.Row="2" FontWeight="Bold" Name="textVerifyPhone" Text="Please enter the phone verification code :" Foreground="#1493D6" VerticalAlignment="Top"/>
                <TextBox Grid.Column="2" Height="25" Grid.Row="2"  Name="PhoneVerificationcode" Margin="0 0 0 10" Text="{Binding PhoneVerificationCode, Mode=TwoWay}" />
                <Button Grid.Column="4" Grid.Row="2" FontWeight="Bold"  HorizontalAlignment="Left" Content="Resend Sms" Cursor="Hand" Command="{Binding Path=ResendPhoneCommand}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <TextBlock TextDecorations="Underline">  
                    <ContentPresenter />
                                </TextBlock>
                            </ControlTemplate>
                        </Button.Template>
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Foreground" Value="Blue" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter Property="Foreground" Value="Red" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,80" Grid.Column="2" Grid.ColumnSpan="2" Grid.Row="5" HorizontalAlignment="Left">
                        <Button Background="#1cb5ef" Style="{DynamicResource RoundCorner}"  Width="100" Foreground="White" Content="Verify Account" Height="40" Grid.Column="4" Grid.Row="5" Margin="2,10" Command="{Binding Path=VerifyTokenCommand}"  Name="VerifyAccount" VerticalAlignment="Top"/>
                        <Button Background="#1cb5ef" Style="{DynamicResource RoundCorner}" HorizontalAlignment="Right" Width="90" Foreground="White" Content="Cancel" Height="40" Grid.Column="5" Grid.Row="5" Margin="12,10" Command="{Binding Path=VerifyTokenCancelCommand}"  Name="Cancel" VerticalAlignment="Top"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Grid>
</UserControl>
