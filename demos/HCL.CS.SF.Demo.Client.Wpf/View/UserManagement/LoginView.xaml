<UserControl x:Class="HCL.CS.SF.DemoClientWpfApp.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local ="clr-namespace:HCL.CS.SF.DemoClientWpfApp" xmlns:UserManagement="clr-namespace:HCL.CS.SF.DemoClientWpfApp.View.UserManagement"
             mc:Ignorable="d">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Grid.Row="0" Grid.Column="1" CornerRadius="2" BorderThickness="2" Margin="5 0 0 0" DockPanel.Dock="Top" BorderBrush="Transparent" >
                <StackPanel>
                    <ContentControl Name="FrameworkNavigation"></ContentControl>

                    <TabControl x:Name="Tabs1" TabStripPlacement="Top">
                        <TabItem Height="0" IsSelected="True" VerticalAlignment="Top" BorderBrush="Transparent">
                            <UserManagement:UserLogin Width="400" Height="300" HorizontalAlignment="Center" />
                        </TabItem>
                    </TabControl>
                </StackPanel>
            </Border>
        </Grid>
    </StackPanel>
</UserControl>
